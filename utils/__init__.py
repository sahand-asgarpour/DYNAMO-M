"""utils package.

================================ RECONSTRUCTED STUB ================================
The original ``utils`` package was never committed to this repository (see AGENTS.md,
which notes it is treated as an external sibling). The modules here are MINIMAL
reconstructions, inferred from how they are consumed across the codebase, created so
that the ``test_runs`` adaptation-only test can execute end-to-end.

They are NOT the original implementations. Behaviour that is not exercised by the
test (GUI, profiling, gravity model, multi-country runs, low-memory caching) is only
stubbed to the extent required to import/return sensible values. Replace this package
with the genuine ``utils`` sources when they become available.
===================================================================================
"""

